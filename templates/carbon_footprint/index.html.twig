{% extends 'base.html.twig' %}

{% block title %}Carbon Footprint Calculator{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Carbon Footprint Calculator</h1>
    
    {% for message in app.flashes('error') %}
        <div class="alert alert-danger">{{ message }}</div>
    {% endfor %}
    
    <div class="card">
        <div class="card-body">
            <form method="post" action="{{ path('carbon_footprint_calculate') }}">
                <div class="mb-3">
                    <label for="transport_mode" class="form-label">Transport Mode:</label>
                    <select class="form-select" id="transport_mode" name="transport_mode" required>
                        <option value="">Select a transport mode</option>
                        {% for key, mode in transportModes %}
                            <option value="{{ key }}">{{ mode }}</option>
                        {% endfor %}
                    </select>
                </div>
                
                <div class="mb-3">
                    <label for="distance" class="form-label">Distance (km):</label>
                    <input type="number" step="0.1" min="0.1" class="form-control" id="distance" name="distance" required>
                </div>
                
                <button type="submit" class="btn btn-primary">Calculate</button>
            </form>
        </div>
    </div>
</div>
{% endblock %}